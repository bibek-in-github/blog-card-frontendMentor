import 'package:flutter/material.dart';

void main() => runApp(blogCard());

class blogCard extends StatelessWidget {
  const blogCard({super.key});

  @override
  Widget build(BuildContext context) {
    return  MaterialApp(
      
    );
  }
}